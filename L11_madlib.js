let noun,verb,adjective,adverb,place

function setup(){
    createCanvas(400,600)
    noun=createInput()
    verb=createInput()
    adjective=createInput()
    adverb=createInput()
    place=create
}
function draw(){
    background(220)
}