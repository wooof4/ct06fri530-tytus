let noun,verb,adjective,adverb,place

function setup(){
    createCanvas(400,600)
    noun=createInput()
    verb=createInput()
    adjective=createInput()
    adverb=createInput()
    place=createInput()
}
function draw(){
    background(220)
}